# Polymorphism - when one thing is used with different meaning - many forms

class whatever:
    pass

# Operator Overloading : When the same operator is allowed to have different meanng according to the content
print(2+2) # 4
print("Long"+"Live") # LongLive - concatenation of strings
print([5,28,6,4] + [45,6,2,8]) # merge
# different meaning of + for diff datatypes

# dunder functions : __add__,__sub__,__mul__,__mod__,__truediv__
#                    a.add(b),a.sub(b),a.mul(b),a.mod(b),a.truediv(b)









